[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "organization-rag"
version = "0.1.0"
description = "Organization document RAG system"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]